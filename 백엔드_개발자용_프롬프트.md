# 백엔드 개발자용 프롬프트

## 📋 사용 방법

1. Python FastAPI 백엔드 프로젝트에서 Claude Code를 실행합니다
2. 이 프롬프트와 함께 `백엔드_FCM_구현_가이드.md` 파일을 업로드합니다
3. Claude Code가 자동으로 코드를 생성하고 구현합니다

---

## 🤖 Claude Code에게 전달할 프롬프트

```
<너의 역할>
Python FastAPI 백엔드 시니어 개발자
</너의 역할>

<프로젝트 컨텍스트>
우리는 웹사이트 크롤링 + AI 정제 + 푸시 알림 서비스를 개발하고 있습니다.
안드로이드 앱에서 FCM 기능 구현이 완료되었고, 이제 백엔드에서 FCM 푸시 발송 기능을 구현해야 합니다.
</프로젝트 컨텍스트>

<데이터베이스 스키마>
// users 테이블
- id: uuid (PK)
- firebase_uid: varchar(255) (unique)
- email: varchar(255)
- name: varchar(100)
- created_at: timestamp
- updated_at: timestamp

// sites 테이블
- id: uuid (PK)
- user_id: uuid (FK -> users.id, nullable)
- url: varchar(1024) (unique)
- name: varchar(255)
- description: text
- category: text
- is_public: boolean (default: false)
- crawl_cycle_time: timestamp
- created_at: timestamp
- updated_at: timestamp

// user_sites 테이블 (사용자-사이트 구독 관계)
- id: uuid (PK)
- user_id: uuid (FK -> users.id)
- site_id: uuid (FK -> sites.id)
- is_subscribed: boolean (default: true)
- created_at: timestamp
- updated_at: timestamp

// posts 테이블
- id: uuid (PK)
- site_id: uuid (FK -> sites.id)
- title: varchar(512)
- content: text
- source_url: varchar(1024)
- event_date: timestamp (nullable)
- location: varchar(255) (nullable)
- created_at: timestamp
- updated_at: timestamp

// calendar_events 테이블
- id: uuid (PK)
- user_id: uuid (FK -> users.id)
- post_id: uuid (FK -> posts.id, nullable)
- title: varchar(255)
- description: text
- start_time: timestamp
- end_time: timestamp (nullable)
- notify_enabled: boolean (default: false)
- notify_time: timestamp (nullable)
- created_at: timestamp
- updated_at: timestamp

// fcm_tokens 테이블
- id: uuid (PK)
- user_id: uuid (FK -> users.id)
- fcm_token: varchar(1024)
- platform: varchar(50) (nullable)
- device_info: varchar(255) (nullable)
- last_used: timestamp (nullable)
- created_at: timestamp
- updated_at: timestamp
</데이터베이스 스키마>

<구현해야 할 기능>
1. FCM 토큰 등록 API (POST /api/v1/fcm/register)
   - 안드로이드 앱에서 로그인 시 FCM 토큰을 전송받아 fcm_tokens 테이블에 저장
   - 기존 토큰이 있으면 last_used 갱신, 없으면 신규 등록

2. 크롤링 완료 시 푸시 알림 발송
   - 크롤링이 완료되면 해당 사이트를 구독한 사용자들에게 FCM 푸시 발송
   - user_sites 테이블에서 is_subscribed=true인 사용자 조회
   - FCM 메시지 타입: "crawling_complete"

3. 새 게시글 감지 시 푸시 알림 발송
   - SimHash 유사도 비교 후 새 게시글이 posts 테이블에 추가되면 FCM 푸시 발송
   - 해당 사이트 구독자들에게 알림
   - FCM 메시지 타입: "new_post"

4. 캘린더 일정 알림 발송
   - calendar_events 테이블에서 notify_enabled=true이고 notify_time이 현재 시각에 도달한 일정 조회
   - 스케줄러를 사용하여 매 1분마다 체크
   - FCM 메시지 타입: "schedule_reminder"

5. /posts/list API 수정
   - 응답에 is_new 필드 추가 (24시간 이내 생성된 게시글은 true)
</구현해야 할 기능>

<기술 스택>
- FastAPI
- SQLAlchemy (또는 raw SQL)
- Firebase Admin SDK
- APScheduler (일정 알림용)
- MySQL
</기술 스택>

<내 지시사항>
업로드한 "백엔드_FCM_구현_가이드.md" 파일을 참고하여 다음 작업을 수행해주세요:

1. 현재 프로젝트 구조를 파악하고 분석
2. Firebase Admin SDK 설정 파일 생성 (firebase_config.py)
3. FCM 토큰 등록 API 구현 (routers/fcm.py)
4. 크롤링 완료 시 푸시 발송 로직을 기존 크롤러 코드에 추가
5. 새 게시글 추가 시 푸시 발송 로직을 기존 코드에 추가
6. 일정 알림 스케줄러 구현 (services/scheduler.py)
7. /posts/list API에 is_new 필드 추가
8. main.py에 라우터 등록 및 스케줄러 시작 코드 추가

각 단계마다 기존 코드를 분석하고, 적절한 위치에 코드를 추가해주세요.
가이드 문서의 코드를 그대로 복사하지 말고, 현재 프로젝트의 코드 스타일과 구조에 맞게 조정해서 작성해주세요.

구현이 완료되면:
- 테스트 방법 안내
- 필요한 환경 변수 또는 설정 파일 목록 제공
- Firebase 서비스 계정 키 발급 방법 안내

step-by-step으로 진행해주세요.
</내 지시사항>
```

---

## 💡 추가 팁

### 1. Firebase 서비스 계정 키가 이미 있는 경우
프롬프트에 추가:
```
<추가 정보>
Firebase 서비스 계정 키 파일이 이미 있습니다: config/firebase-service-account.json
</추가 정보>
```

### 2. 기존 크롤러 파일 위치를 아는 경우
프롬프트에 추가:
```
<프로젝트 구조>
- 크롤러 로직: app/services/crawler.py
- 게시글 추가 로직: app/services/post_processor.py
- API 라우터: app/routers/
- 메인 앱: app/main.py
</프로젝트 구조>
```

### 3. 특정 ORM이나 DB 라이브러리를 사용하는 경우
프롬프트에 추가:
```
<기술 스택 상세>
- ORM: SQLAlchemy 2.0 (async)
- DB 연결: asyncpg
- DB: PostgreSQL (또는 MySQL)
</기술 스택 상세>
```

---

## 🔥 프롬프트 사용 예시

백엔드 개발자는 다음과 같이 진행하면 됩니다:

1. **백엔드 프로젝트 폴더로 이동**
   ```bash
   cd /path/to/backend-project
   ```

2. **Claude Code 실행**
   ```bash
   claude-code
   ```

3. **파일 업로드 + 프롬프트 입력**
   - 위의 프롬프트를 복사해서 붙여넣기
   - `백엔드_FCM_구현_가이드.md` 파일을 드래그 앤 드롭으로 업로드

4. **Claude Code가 자동으로 구현**
   - 프로젝트 구조 분석
   - 필요한 파일 생성
   - 기존 파일 수정
   - 테스트 방법 안내

---

## ⚡ 빠른 시작용 간단 프롬프트

시간이 없다면 이 짧은 버전을 사용하세요:

```
FastAPI 백엔드에 FCM 푸시 알림 기능을 구현해줘.

업로드한 "백엔드_FCM_구현_가이드.md" 파일을 참고해서:
1. Firebase Admin SDK 설정
2. FCM 토큰 등록 API 구현
3. 크롤링 완료, 새 게시글, 일정 알림 푸시 발송 구현
4. /posts/list API에 is_new 필드 추가

현재 프로젝트 구조를 분석하고, 기존 코드에 적절히 통합해줘.
```

---

## 📂 백엔드 개발자에게 전달할 파일

다음 2개 파일을 전달하세요:

1. ✅ `백엔드_FCM_구현_가이드.md` - 상세 구현 가이드
2. ✅ `백엔드_개발자용_프롬프트.md` - 이 파일 (Claude Code 사용법)

**전달 방법**:
- 이메일 첨부
- Slack/Discord 업로드
- Git 리포지토리의 docs/ 폴더에 커밋

---

**작성일**: 2025-11-18
